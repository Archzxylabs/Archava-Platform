{
  "name": "@archava/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Replaceable provider ports for presence transport, brain, and embodiment, plus the fail-closed registry that resolves them per tenant.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@archava/config": "workspace:*",
    "@archava/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}

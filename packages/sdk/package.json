{
  "name": "@archava/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The embeddable page-awareness SDK: DOM observers that emit context events, a consent-gated collector, and a zero-copy bridge that hands the assembled graph to the assistant.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --reporter=default"
  },
  "dependencies": {
    "@archava/config": "workspace:*",
    "@archava/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}

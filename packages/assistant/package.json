{
  "name": "@archava/assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The deterministic turn pipeline: context in, masked and gated turn out, with structured truth resolved ahead of retrieval and every action re-checked by the policy gate.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@archava/acl": "workspace:*",
    "@archava/adapters": "workspace:*",
    "@archava/config": "workspace:*",
    "@archava/core": "workspace:*",
    "@archava/knowledge": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}

{
  "name": "@archava/chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The chat shell that draws a decided turn into a shadow root: confirmation cards, source cards, and a structured-truth panel that never restates a price as prose.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@archava/assistant": "workspace:*",
    "@archava/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}

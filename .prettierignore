# Generated, and owned by pnpm rather than by a person. Formatting it would
# rewrite bytes on every install that changes the lockfile, so the file is not
# a formatting target — `pnpm format:check` checks what a person edits.
pnpm-lock.yaml

# Build output. Compiled bundles are not source, and a formatter run over them
# produces noise on files nobody reads.
dist/
node_modules/

{
  "name": "@archava/configurator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The configurator core: intake to recommended scope, deterministic variants, budget verdicts, and the normalized block a quote and a client config share.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "echo \"@archava/configurator ships TypeScript source (no build step)\""
  },
  "dependencies": {
    "@archava/config": "workspace:*",
    "@archava/pricing": "workspace:*",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
